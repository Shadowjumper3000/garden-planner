{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build & Start All (dev)",
            "type": "shell",
            "command": "docker compose -f infra/docker-compose.dev.yml up --build",
            "problemMatcher": [],
            "group": "build",
            "detail": "Builds and starts all services in development mode using Docker Compose."
        }
    ]
}