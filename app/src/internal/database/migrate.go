package database

















}	return err	_, err := db.Exec(query)	`	);		email TEXT NOT NULL UNIQUE		name TEXT NOT NULL,		id SERIAL PRIMARY KEY,	CREATE TABLE IF NOT EXISTS users (	query := `func AutoMigrate(db *sql.DB) error {)	"database/sql"import (package database