# Development defaults — safe to commit, used only by local dev tasks.
# Override any value by setting it in your shell before running docker compose.
# Production NEVER uses this file.

POSTGRES_USER=garden
POSTGRES_PASSWORD=garden_dev
POSTGRES_DB=garden

JWT_SECRET=dev_jwt_secret_not_for_production

ADMIN_EMAIL=admin@localhost
ADMIN_PASSWORD=adminpassword
ADMIN_NAME=Admin

GF_ADMIN_USER=admin
GF_ADMIN_PASSWORD=admin
